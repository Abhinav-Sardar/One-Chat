import { useRef, FC as FunctionalComponent } from "react";
import { Page } from "../Styled-components/NotFound.styled";

import io from "socket.io-client";
const CreateRoom__Wrapper: FunctionalComponent = () => {
  return (
    <Page>
      <h1>lorem10000</h1>
    </Page>
  );
};
export default CreateRoom__Wrapper;
